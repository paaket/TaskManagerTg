#pragma once
#include <string>

struct Config {
	std::string botApi = "";
	std::string host = "host=";
	std::string port = "port=";
	std::string dbName = "dbname=";
	std::string user = "user=";
	std::string password = "password=";
	std::string text = host + " " + port + " " + dbName + " " + user + " " + password;
};